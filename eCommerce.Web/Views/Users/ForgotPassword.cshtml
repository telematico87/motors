@model eCommerce.Web.ViewModels.ForgotPasswordViewModel
@{
    Model.PageTitle = "PP.ForgotPassword.Title".LocalizedString();
    Model.PageDescription = "PP.ForgotPassword.Description".LocalizedString().Replace("{application}", ConfigurationsHelper.ApplicationName);
    Model.PageURL = Url.ForgotPassword();
    Model.PageImageURL = PictureHelper.PageImageURL("forgot-password.jpg");
}

<div class="register-login-section spad">
    <div class="container" id="forgot-password-container">
        <div class="row">
            <div class="col-lg-6 offset-lg-3">
                <div class="login-form page-content-holder">
                    <div class="page-content-header">
                        <h2>@Model.PageTitle</h2>
                    </div>
                    @Html.Partial("_ForgotPassword", Model)
                    <div class="switch-login">
                        <a href="@Url.Register()" class="or-login">
                            @("PP.Login.RegisterAccount".LocalizedString())
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>