@model eCommerce.Web.Areas.Dashboard.ViewModels.MedidasListingViewModel

@{
    if (Model != null)
    {
        Model.PageTitle = "Dashboard.Medida.Title".LocalizedString();
        Model.PageDescription = "Dashboard.Medida.Description".LocalizedString();
        Model.PageURL = Url.ListAction("Medida");
        Model.PageImageURL = PictureHelper.PageImageURL("color.jpg");
    }
}


<div class="page-info-container">
    <h3 class="h3 mb-2 text-gray-800">
        @Model.PageTitle
    </h3>
    <p>
        @Model.PageDescription
    </p>
    <hr />
</div>

<div class="page-data-holder card shadow mb-4">
    <div class="card-header">
        <form action="@Url.ListAction("Medida")">
            <div class="input-group">
                <input name="searchterm" type="text" value="@Model.SearchTerm" class="form-control" placeholder="@("Dashboard.Medida.SearchPlaceholder".Localized())">
                <button type="submit" class="form-control btn btn-primary"><i class="fas fa-search"></i> @("Dashboard.Medida.Search".Localized())</button>
                <a href="@Url.ListAction("Medida")" role="button" class="form-control btn btn-primary"><i class="fas fa-redo-alt"></i> @("Dashboard.Medida.Reset".Localized())</a>
                <a href="@Url.CreateAction("Medida")" role="button" class="form-control btn btn-success"><i class="fas fa-plus"></i> @("Dashboard.Medida.CreateColor".Localized())</a>
            </div>
        </form>
    </div>
    <div class="card-body p-0" id="list-holder">
        @{Html.RenderPartial("_Listing", Model);}
    </div>
</div>

